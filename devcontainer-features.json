{
    "features": [
        {
            "id": "pax",
            "name": "Pax Modpack Manager",
            "options": {
                "version": {
                    "type": "string",
                    "proposals": [ "latest" ],
                    "default": "latest",
                    "description": "Select a version"
                }
            },
            "include": ["ubuntu"]
        }
    ]
}
